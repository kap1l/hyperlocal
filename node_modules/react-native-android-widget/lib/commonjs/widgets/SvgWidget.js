"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.SvgWidget = SvgWidget;
var _reactNative = require("react-native");
var _clickAction = require("./utils/click-action");
var _style = require("./utils/style.utils");
function SvgWidget(_) {
  return null;
}
SvgWidget.__name__ = 'SvgWidget';
SvgWidget.convertProps = props => {
  const svg = typeof props.svg === 'string' ? props.svg : _reactNative.Image.resolveAssetSource(props.svg).uri;
  return {
    ...(0, _style.convertCommonStyle)(props.style ?? {}),
    ...(0, _clickAction.convertClickAction)(props),
    ...(typeof props.svg === 'string' && !props.svg.startsWith('http') ? {
      svgString: svg
    } : {}),
    ...(typeof props.svg === 'string' && props.svg.startsWith('http') ? {
      svgUrl: svg
    } : {}),
    ...(typeof props.svg !== 'string' ? {
      svgUrl: svg
    } : {})
  };
};
//# sourceMappingURL=SvgWidget.js.map