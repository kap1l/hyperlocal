import { Image } from 'react-native';
import { convertClickAction } from './utils/click-action';
import { convertCommonStyle } from './utils/style.utils';
export function ImageWidget(_) {
  return null;
}
ImageWidget.__name__ = 'ImageWidget';
ImageWidget.convertProps = props => {
  return {
    ...convertCommonStyle(props.style ?? {}),
    ...convertClickAction(props),
    imageHeight: props.imageHeight,
    imageWidth: props.imageWidth,
    image: typeof props.image === 'number' ? Image.resolveAssetSource(props.image) : {
      uri: props.image
    },
    ...(props.radius ? {
      radius: props.radius
    } : {})
  };
};
//# sourceMappingURL=ImageWidget.js.map