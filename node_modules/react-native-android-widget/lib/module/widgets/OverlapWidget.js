import { convertClickAction } from './utils/click-action';
import { convertCommonStyle } from './utils/style.utils';
export function OverlapWidget(_ref) {
  let {
    children
  } = _ref;
  return children;
}
OverlapWidget.__name__ = 'FrameLayoutWidget';
OverlapWidget.convertProps = props => {
  var _props$style;
  return {
    ...convertCommonStyle(props.style ?? {}),
    ...convertClickAction(props),
    ...(props !== null && props !== void 0 && (_props$style = props.style) !== null && _props$style !== void 0 && _props$style.overflow ? {
      overflow: props.style.overflow
    } : {})
  };
};
//# sourceMappingURL=OverlapWidget.js.map