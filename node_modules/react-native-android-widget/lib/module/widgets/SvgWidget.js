import { Image } from 'react-native';
import { convertClickAction } from './utils/click-action';
import { convertCommonStyle } from './utils/style.utils';
export function SvgWidget(_) {
  return null;
}
SvgWidget.__name__ = 'SvgWidget';
SvgWidget.convertProps = props => {
  const svg = typeof props.svg === 'string' ? props.svg : Image.resolveAssetSource(props.svg).uri;
  return {
    ...convertCommonStyle(props.style ?? {}),
    ...convertClickAction(props),
    ...(typeof props.svg === 'string' && !props.svg.startsWith('http') ? {
      svgString: svg
    } : {}),
    ...(typeof props.svg === 'string' && props.svg.startsWith('http') ? {
      svgUrl: svg
    } : {}),
    ...(typeof props.svg !== 'string' ? {
      svgUrl: svg
    } : {})
  };
};
//# sourceMappingURL=SvgWidget.js.map